# 📁 styles

Contient les styles globaux, animations, variables CSS, etc.

- `index.css` → Point d’entrée avec `@tailwind` directives
- `theme.css` → Variables CSS ou tokens
- `animations.css` → Animations custom (si besoin)

🎨 Le style local aux composants reste géré par **Tailwind** dans les fichiers `.tsx`.