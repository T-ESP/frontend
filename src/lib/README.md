# 📁 lib

Bibliothèque interne et utilitaires génériques.

Contenu typique :
- Wrapper d’API (ex : `http.ts` avec Axios + interceptors)
- Formatters (`formatDate`, `formatPrice`)
- Fonctions helpers (`classNames`, `downloadFile`, etc.)

🔗 Ce dossier ne contient **aucune dépendance métier**.
