# 📁 providers

Contient tous les **context providers globaux**.

Exemples :
- `ThemeProvider` (dark/light mode)
- `AuthProvider` (utilisateur connecté)
- `ToastProvider`, `ModalProvider`, etc.

🎯 Chaque provider est utilisé dans `main.tsx` pour envelopper l’app.